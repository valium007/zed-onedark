# zed-onedark-theme

Place `onedark.json` in `~/.config/zed/themes`, restart Zed and select it from the command palette (`Ctrl/Cmd+Shift+P`) or (`Ctrl/Cmd+K Ctrl/Cmd+T`).

## Screenshots

![OneDark](screenshots/image.png)
